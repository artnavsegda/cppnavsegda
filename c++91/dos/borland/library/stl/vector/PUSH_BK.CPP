#include <iostream.h>
#include <vector.h>

int main()
{
	vector<int> v;
	v.push_back(44);
	cout << "Vector v element 0 is " << v[0] << endl;
	cout << "Vector v size is " << v.size() << endl;
	return 0;
}
